This folder contains cpp files, that serve as code samples - for displaying/saving the panorama videos.

launch ./compile.sh to compile all the three code samples.
Press 'ESC' to close the window. If the video record has begun, it will end the capture before closing the app

--> 004_laser_scan
    Displays 360 panorama with laser scan overlayed on the image.
